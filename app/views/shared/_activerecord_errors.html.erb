<div id="error_explanation">
  <h2><%= pluralize(active_record.errors.count, "error") %> prohibited the changes from being saved:</h2>
  <ul>
  <% active_record.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
</div>
