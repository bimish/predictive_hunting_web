HuntingLocationTableHelper.delete(<%=@hunting_location.id%>);
if (typeof g_huntingLocationAjaxNotifications != 'undefined') {
  g_huntingLocationAjaxNotifications.notify('delete', <%== @hunting_location.to_json %>);
}
