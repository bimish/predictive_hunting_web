You have been invited by <%= @invite_from%> to join the <%= @plot_name %> hunting plot.

Click on the link below to accept the invitation

<%= confirm_hunting_plot_user_access_request_path(id: @invite_id, only_path: false) %>
