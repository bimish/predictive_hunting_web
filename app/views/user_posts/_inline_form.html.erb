<%= bootstrap_form_for @user_post, layout: :horizontal, remote: true do |f| %>
  <% if @user_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_post.errors.count, "error") %> prohibited this user_post from being saved:</h2>
      <ul>
      <% @user_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_field :post_content %>
  <%= f.select :visibility, get_visibility_list_items, { label:'Visibility', include_blank:'[Visibility]' } %>
  <div class="actions">
    <%= f.submit "OK", class: "btn btn-primary" %>
    <%= link_to "Cancel", "#", onclick: 'javascript: closeAjaxForm(); return false;', class: "btn btn-default" %>
  </div>
<% end %>
