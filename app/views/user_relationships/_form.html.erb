<%= bootstrap_form_for(@user_relationship) do |f| %>
  <% if @user_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_relationship.errors.count, "error") %> prohibited this user_relationship from being saved:</h2>
      <ul>
      <% @user_relationship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.select :related_user_id, get_related_user_list_items, { label:'Related user', include_blank:'[Related user]' } %>
  <%= f.select :relationship_type, get_relationship_type_list_items, { label:'Relationship type', include_blank:'[Relationship type]' } %>
  <div class="actions">
    <%= f.submit class:'btn btn-default btn-primary' %>
  </div>
<% end %>
