(function() {
  var form = $('#form-placeholder').html('<%= j render :partial => 'select', :layout => '/layouts/modal_form_dialog', locals: { include_header_footer: false, title: "Select Members of " + @user_group.get_display_name, user_group: @user_group, users: @users } %>');
  initializeStyledCheckboxes(form);
  showAjaxModalForm('modal-form-dialog');
})();
