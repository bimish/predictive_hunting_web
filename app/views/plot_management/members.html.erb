<% provide(:page_id, :members) %>
<% provide(:page_label, 'Members') %>

<div class="panel panel-default">
  <div class="panel-heading">
    <span class="panel-title">Members |</span>
    <%= link_to combine_tags(bootstrap_glyphicon('plus-sign'), ' Invite Member'), new_hunting_plot_user_access_request_path(@hunting_plot), { remote: true, class:'btn btn-link btn-sm' } %>
  </div>
  <div class="panel-body">
    <%= render partial:'hunting_plot_user_accesses/index_table', :locals => { :hunting_plot_user_accesses => @hunting_plot_user_accesses } %>
  </div>
</div>
